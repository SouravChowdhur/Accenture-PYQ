#include<iostream>
#include<vector>
using namespace std;


char solve(string str){
    vector<int> freq(26, 0);
    for(int i=0; i<str.length(); i++){
        if(freq[str[i] - 97] == 1)
           return str[i];
        else
           freq[str[i] - 97]++;
    }
    return str[0];
}


int main(){
    string str;
    cin>>str;
    cout<<solve(str);
    return 0;
}
